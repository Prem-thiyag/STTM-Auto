-- Generated by sqlx-etl-generator · DO NOT EDIT BY HAND
-- Bootstrap reset for database: {{ database }}
-- Source: metadata/schema/{{ schema_source }}
--
-- Truncates every table this schema declares, respecting foreign-key dependency order
-- (children before parents) so no FK violation is possible regardless of run order.
-- This script only ever runs when a human executes it manually — see bootstrap/README.md.

{% for t in reset_order %}
TRUNCATE TABLE {{ t }} CASCADE;
{% endfor %}
