-- Generated by sqlx-etl-generator · DO NOT EDIT BY HAND (except to fill in TODOs below)
-- Sample data STUB for database: {{ database }}
-- Source: metadata/schema/{{ schema_source }}
--
-- No sample data input was declared for this Generate run (Source Schema, Target Schema,
-- STTM Workbook, User Defined Functions, Folder Hierarchy do not include row-level sample
-- data). Per ADR-001 §5/§9, this generator does NOT fabricate business data — inventing
-- plausible-looking rows would misrepresent synthetic content as real example data.
--
-- Fill in real or synthetic values below, or supply a sample-data input in a future run
-- if this input type is added (see docs/FUTURE_ENHANCEMENTS.md).

{% for t in tables %}
-- TODO: populate sample rows for {{ t.name }} ({{ t.columns | map(attribute='name') | join(', ') }})
-- INSERT INTO {{ t.name }} ({{ t.columns | map(attribute='name') | join(', ') }}) VALUES
--     (/* {{ t.columns | length }} value(s) here */);

{% endfor %}
